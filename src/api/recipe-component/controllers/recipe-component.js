'use strict';

/**
 * recipe-component controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::recipe-component.recipe-component');
