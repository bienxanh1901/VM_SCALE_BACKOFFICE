'use strict';

/**
 * scheduled-job-component controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::scheduled-job-component.scheduled-job-component');
